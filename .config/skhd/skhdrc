# focus window
alt - h : yabai -m window --focus west || yabai -m window --focus stack.prev || yabai -m display --focus west
alt - j : yabai -m window --focus south || yabai -m display --focus south
alt - k : yabai -m window --focus north || yabai -m display --focus north
alt - l : yabai -m window --focus east || yabai -m window --focus stack.next || yabai -m display --focus east

# swap managed window (fallback to focusing the adjacent display; the
# `window --display` cross-display move needs the SA so we drop it).
shift + alt - h : yabai -m window --swap west || yabai -m display --focus west
shift + alt - j : yabai -m window --swap south || yabai -m display --focus south
shift + alt - k : yabai -m window --swap north || yabai -m display --focus north
shift + alt - l : yabai -m window --swap east || yabai -m display --focus east

# move managed window
shift + alt + ctrl - h : yabai -m window --warp west
shift + alt + ctrl - j : yabai -m window --warp south
shift + alt + ctrl - k : yabai -m window --warp north
shift + alt + ctrl - l : yabai -m window --warp east

alt + cmd - h : yabai -m window west --resize right:-100:0 2> /dev/null || yabai -m window --resize right:-100:0
alt + cmd - j : yabai -m window north --resize bottom:0:100 2> /dev/null || yabai -m window --resize bottom:0:100
alt + cmd - k : yabai -m window south --resize top:0:-100 2> /dev/null || yabai -m window --resize top:0:-100
alt + cmd - l : yabai -m window east --resize left:100:0 2> /dev/null || yabai -m window --resize left:100:0

# balance size of windows
alt + cmd - 0 : yabai -m space --balance

# Smart grow/shrink (aerospace alt-minus/equal analog). yabai doesn't expose
# the parent split orientation cleanly, so we cascade: try horizontal (right,
# then left for right-edge windows); if both fail we must be in a pure
# vertical split, so fall back to vertical (bottom, then top). 0x1B=- 0x18==.
alt - 0x1B : yabai -m window --resize right:-50:0 2>/dev/null \
           || yabai -m window --resize left:50:0   2>/dev/null \
           || yabai -m window --resize bottom:0:-50 2>/dev/null \
           || yabai -m window --resize top:0:50
alt - 0x18 : yabai -m window --resize right:50:0  2>/dev/null \
           || yabai -m window --resize left:-50:0 2>/dev/null \
           || yabai -m window --resize bottom:0:50 2>/dev/null \
           || yabai -m window --resize top:0:-50

# rotate tree
alt - r : yabai -m space --rotate 90

#focus recent
alt - u : yabai -m window --focus recent

# toggle window fullscreen zoom
alt - f : yabai -m window --toggle zoom-fullscreen

# Removed: sticky/pip toggles need the SA.

# toggle padding and gap
alt - g : yabai -m space --toggle padding; yabai -m space --toggle gap

# float / unfloat window and center on screen
alt - t : yabai -m window --toggle float;\
          yabai -m window --grid 20:20:1:1:18:18

# alt - p / alt - m / alt - b moved: now app launchers (see bottom).
# macOS cmd-m still minimizes.

# toggle window split type
alt - e : yabai -m window --toggle split

# Space switching: use macOS's native Switch-to-Desktop-N shortcuts
# (ctrl+1..9), enabled in System Settings → Keyboard → Shortcuts → Mission
# Control. Hammerspoon-based alt+N / alt+shift+N bindings were removed —
# hs.spaces.moveWindowToSpace is broken on macOS 26 (returns success but
# no-ops). No SIP-free way to move windows between spaces currently works.
# Create new spaces via Mission Control (ctrl+up, "+") instead.

# Reload sketchybar and kill yabai/skhd; launchd KeepAlive=true relaunches
# them with the fresh configs. Simpler and more reliable than the multi-line
# launchctl kickstart chain, which skhd's parser was mishandling.
ctrl + alt - r : sketchybar --reload; pkill -x yabai; pkill -x skhd

# toggle bsp/stack layout (moved from alt-b since that now launches Chrome).
ctrl + alt - b : yabai -m space --layout $(yabai -m query --spaces --space | jq -r '.type' | grep -q bsp && echo stack || echo bsp)

# Application launchers (matching aerospace.toml bindings)
alt - i : open -a "Ghostty"
alt - s : open -a "Slack"
alt - m : open -a "Gmail"
alt - c : open -a "Google Calendar"
alt - o : open -a "Obsidian"
alt - x : open -a "Cursor"
alt - p : open -a "Zen"
alt - b : open -a "Google Chrome"
alt - y : open -a "YouTube"

